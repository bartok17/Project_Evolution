package agh.ics.oop.model;


import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
class AnimalTest {
    @Test
    void check_movement()
    {

        //old

        /*Animal animal = new Animal(new Vector2d(4,4));
        animal.setDirection(MapDirection.NORTH);
        animal.move(MoveDirection.FORWARD,0,4);
        assertEquals(new Vector2d(4,4), animal.getCoordinates());
        animal.move(MoveDirection.RIGHT,0,4);
        animal.move(MoveDirection.RIGHT,0,4);
        animal.move(MoveDirection.FORWARD,0,4);
        assertEquals(new Vector2d(4,3), animal.getCoordinates());
        animal.move(MoveDirection.BACKWARD,0,4);
        assertEquals(new Vector2d(4,4), animal.getCoordinates());
        assertEquals(MapDirection.SOUTH, animal.getDirection());*/

    }
  
}