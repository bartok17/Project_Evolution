package agh.ics.oop;

import agh.ics.oop.model.MoveDirection;
import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class OptionsParserTest {


    //Tests Obsolete, New ones are not need since everything is checked in SimulationTests
   /* @Test

    void parseMoveDirection() {
        //Given
        String[] moveDirections = {"l","r","k","","ll","l r","f","b"};
        List<MoveDirection> correctResult = Arrays.asList(MoveDirection.LEFT,MoveDirection.RIGHT,MoveDirection.FORWARD,MoveDirection.BACKWARD);
        List<MoveDirection> result = OptionsParser.ParseMoveDirection(moveDirections);
        //then
        assertEquals(correctResult, result);
        }
*/
}
